# Copy to .env for running the app with `uv run fastapi dev app/main.py`
# outside of docker compose (e.g. against a locally published db port).
DATABASE_URL=postgresql+asyncpg://todo:todo@localhost:5432/todo
